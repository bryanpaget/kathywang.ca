# Kathy Wang Photography Gallery

This is a photo gallery website built with Hugo and deployed on GitHub Pages.

## Development

To run the site locally:

```bash
hugo server
```

To build the site:

```bash
hugo
```

## Deployment

The site is automatically deployed to GitHub Pages when changes are pushed to the main branch.